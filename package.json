{
  "name": "oras-ai-assistant",
  "version": "0.2.1",
  "private": true,
  "description": "Quality tooling for the ORAS AI Assistant WordPress plugin.",
  "scripts": {
    "lint:php": "php tools/lint-php.php",
    "lint:js": "node --check assets/scanner.js",
	"test": "php tools/run-tests.php && node tests/frontend-chat-test.js",
    "quality": "npm run lint:php && npm run lint:js && npm test"
  }
}
